import Header from "@/components/global/Header";

const route = () => {
	return (
		<div className="flex flex-col p-5">
			<Header />
			<div className="flex justify-center items-center h-screen">Route Page</div>
		</div>
	);
};

export default route;
